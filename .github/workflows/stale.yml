name: 'Close stale issues'
on:
  schedule:
    - cron: '30 1 * * *'
permissions: {}

jobs:
  stale:
    permissions:
      contents: read
      issues: write
      pull-requests: write
    uses: cpp-linter/.github/.github/workflows/stale.yml@2343264e1cf33bc688e01d962323d5cb70fe1905 # main
